const OTP_TYPE = Object.freeze({
    MOBILE: "mobile",
    EMAIL: "email",
  });

const USER_ROLE = Object.freeze({
    ADMIN: "admin",
    USER: "user",
  });


module.exports = {
    OTP_TYPE,
    USER_ROLE
}